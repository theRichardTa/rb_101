# have an array of integers
# define a new empty array
# want the value of every other element starting from index 0; therefore every even index
# take the value of every even index in old array and put into new array
# print new array